/**
 * @jest-environment node
 */

// const APIServer = require("../app/siphon-api-server");


test('Does the siphone-api-server class construct', done => {

    jest.setTimeout(1500);
    // expect.assertions(1);

    done();
    // function callback(data) {
    //     try {
    // // console.log(myTestServer.status);
    //         expect(myTestServer instanceof APIServer).toBeTruthy();
    //         done();
    //     } catch (error) {
    //         done(error);
    //     }
    // }
    //
    //
    // let myTestServer = new APIServer(4443);
    // // console.log(myTestServer.status);
    // myTestServer.startListening(callback);
});

